"use client";

export const dynamic = "force-dynamic";
export const revalidate = 0;
export const fetchCache = "force-no-store";

export default function BilancioMensilePage() {
  return (
    <h1 style={{ color: "green" }}>
      BILANCIO MENSILE — TEST OK
    </h1>
  );
}
