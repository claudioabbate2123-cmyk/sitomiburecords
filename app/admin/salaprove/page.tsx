"use client";

export const dynamic = "force-dynamic";
export const revalidate = 0;
export const fetchCache = "force-no-store";

export default function SalaProvePage() {
  return (
    <h1 style={{ color: "blue" }}>
      SALA PROVE — TEST OK
    </h1>
  );
}
